# Data Scope & Exploration Findings

**Dataset:** AIT Log Data Set V2.0 — russellmitchell testbed  
**Source:** https://zenodo.org/records/5789064  
**Database:** PostgreSQL  
**Date:** February 6, 2026  
**Status:** Deep exploration complete — scope confirmed and validated by parsing all data

---

## 1. Dataset Overview

The russellmitchell testbed simulates a small enterprise network ("Smith" at russellmitchell.com) with 22 hosts over a 4-day period (Jan 21-25, 2022). A multi-stage cyberattack occurs on Jan 24, 03:01–13:50 UTC (~10 hours including DNS exfiltration). The dataset contains raw logs from all hosts and ground truth labels for attack-related events.

The dataset is generated by the AIT Kyoushi simulation framework, which defines **3 distinct attack scenarios** in `environment/datasets/`. The raw log data in `gather/` is shared across all scenarios (same network, same employees, same background traffic), but the attack scope, labeling rules, and processed outputs differ per scenario. Our data uses the **full "scenario"** — the richest configuration with all attack phases labeled.

### Scenario Comparison

| Aspect | scenario (full) — OURS | scenario1 (no exfil) | scenario2 (exfil only) |
|--------|------------------------|---------------------|----------------------|
| Foothold attack | Yes | Yes | No |
| Escalation | Yes | Yes | No |
| DNS exfiltration | Yes | No | Yes |
| Labeling rules | All 6 | 5 (no dnsmasq) | 2 (audit + dnsmasq only) |
| Labeled lines | 61,862 | ~7,827 | ~54,046 |
| Log sources with labels | 8 | 7 | 3 |
| CPU monitoring | Yes | Yes | No |

**Why the full scenario:** It gives us the most labeled data, the most complex attack chain, and the most log types to design tables around. Under scenario1, we'd lose all 54K DNS exfiltration labels. Under scenario2, we'd lose the entire web attack/escalation narrative and have only 3 log sources.

---

## 2. Verified Directory Structure

```
russellmitchell/
├── dataset.yaml                    ← Simulation window: 2022-01-21 to 2022-01-25
├── gather/                         ← Raw logs from 22 hosts
├── labels/                         ← Ground truth for 5 hosts, 8 log files
├── rules/                          ← 6 YAML labeling rule definitions
├── processing/                     ← Labeling pipeline config (matches full scenario)
└── environment/                    ← Kyoushi testbed deployment code + 3 scenario definitions
```

### Hosts in `gather/` (22 total, from `processing/config/servers.yaml`)

| Host | Type | Network Zone |
|------|------|-------------|
| attacker_0 | Attacker machine | External |
| inet-firewall | Edge firewall | DMZ |
| inet-dns | External DNS server | DMZ |
| intranet_server | WordPress/intranet (MAIN TARGET) | Internal |
| vpn | VPN gateway | DMZ |
| internal_share | File server | Internal |
| mail | Internal mail server | Internal |
| cloud_share | Cloud storage | Internal |
| monitoring | Logstash/metrics | Internal |
| webserver | Public web server | DMZ |
| internal_employee_0–3 | Employee workstations (4) | Internal |
| remote_employee_0–2 | Remote VPN users (3) | External |
| ext_user_0–2 | External simulated users (3) | External |
| davey_mail, morris_mail | External mail servers (2) | External |

### Attacker Profile (from `processing/config/attacker/attacker.yaml`)

| Property | Value |
|----------|-------|
| Hostname | attacker-0 |
| External IP | 192.168.230.122 |
| VPN-assigned internal IP | 172.19.131.174 |
| Compromised VPN user | jhall (192.168.230.165) |
| Escalation target user | jhall (password: cracked via WordPress hash) |
| Web service user (webshell) | www-data (uid 33) |

The attacker operates from 2 IPs — the external IP (192.168.230.122) for VPN connection, and the VPN-assigned IP (172.19.131.174) visible in Apache, DNS, and audit logs inside the network. Cross-log correlation confirmed the attacker appears across 4 of the 8 labeled log sources.

### Corrections from Online Reference

| Online Reference | Actual |
|-----------------|--------|
| `firewall_0/` | `inet-firewall/` |
| `wordpress_server/` | Does not exist — WordPress is on `intranet_server` |
| `dns_server/` | `inet-dns/` |
| `file_share/` | `internal_share/` |
| `vpn_server/` | `vpn/` |
| `workstation_0, 1/` | `internal_employee_0–3/` |
| `intranet_server/logs/dns/query.log` | Does not exist — DNS logs are on `inet-firewall` and `inet-dns` |

---

## 3. Ground Truth Labels

Only **5 of 22 hosts** have label files. Only **8 specific log files** have attack annotations. This is critical for scoping — we focus where labels exist.

### Label Format (JSONL)

Each line maps a raw log line number to attack labels:

```json
{"line": 1860, "labels": ["attacker_change_user", "escalate"], "rules": {"attacker_change_user": ["attacker.escalate.audit.su.login"], "escalate": ["attacker.escalate.audit.su.login"]}}
```

- `line`: line number in corresponding raw log file
- `labels`: attack phase tags (can be multi-valued — events frequently have 2-3 co-occurring labels)
- `rules`: labeling rule that triggered the tag

Unlabeled log lines = normal (non-attack) behavior.

### Labeling Rules (6 files)

| Rule File | Covers | Scenarios Active In |
|-----------|--------|-------------------|
| `0_auth.yaml` | auth.log patterns (su, sudo, session) | scenario, scenario1 |
| `apache.yaml` | HTTP access/error (dirb, wpscan, webshell) | scenario, scenario1 |
| `audit.yaml` | Kernel audit (su login, escalation) | scenario, scenario1, scenario2 |
| `dnsmasq.yaml` | DNS exfiltration (dnsteal domain matching) | scenario, scenario2 |
| `monitoring.yaml` | CPU spikes during password cracking | scenario, scenario1 |
| `openvpn.yaml` | VPN connections from attacker IP | scenario, scenario1 |

---

## 4. Data Volume — Verified by Parsing (0% parse errors across all files)

### Labeled Log Files (What We're Working With)

| Host | Log File | Raw Lines | Labeled Lines | % Labeled |
|------|----------|----------:|-------------:|----------:|
| inet-firewall | dnsmasq.log | 275,900 | 54,035 | 19.6% |
| intranet_server | apache2/access.log.2 | 8,530 | 7,695 | 90.2% |
| intranet_server | apache2/error.log.2 | 36 | 36 | 100% |
| intranet_server | audit/audit.log | 2,316 | 9 | 0.4% |
| intranet_server | auth.log | 272 | 8 | 2.9% |
| vpn | openvpn.log | 5,537 | 28 | 0.5% |
| internal_share | audit/audit.log | 732 | 2 | 0.3% |
| monitoring | cpu.log | 1,920 | 49 | 2.6% |
| **TOTAL** | | **295,243** | **61,862** | **20.9%** |

**Data quality:** All 8 files parsed with 0 parse errors and 0 timestamp nulls. The data is clean.

### Attacker Timeline

`gather/attacker_0/logs/attacks.log`: **55 action entries** — ground truth timeline of every attacker action.
- **Attack window:** 2022-01-24 03:01:00 to 2022-01-24 13:50:03 (10h 49m including DNS exfiltration)
- **Foothold phase:** 03:01 – 03:59 (VPN connect → recon → WordPress exploit → webshell → recon commands)
- **Password cracking:** 03:59 – 04:36 (~37 min, detectable as CPU spike in monitoring data)
- **Escalation phase:** 04:36 – 04:38 (reverse shell → su to jhall → sudo shadow read → ifconfig)
- **DNS exfiltration:** runs independently, stops at 13:50

### Project Requirement Check

| Requirement | Threshold | Our Data | Status |
|-------------|-----------|----------|--------|
| Total rows across all tables | 50,000+ | 295,243 raw + 61,862 labels | EXCEEDS |
| Attributes | 10+ | 46 unique across all log types | EXCEEDS |
| Tables | 4+ | 8 planned | EXCEEDS |
| Time attributes | Required | Every log has timestamps | PASS |
| Categorical attributes | Required | action, event_type, method, level, query_type, username, ... | PASS |
| Numeric attributes | Required | status_code, bytes_sent, pid, uid, cpu_total_pct, client_port, audit_serial, ... | PASS |
| Requires normalization | Required | Yes — 7 different log formats | PASS |
| Each table has 100s–1000s of rows | Required | Smallest: 36 (error.log), Largest: 275,900 (DNS) | PASS |

---

## 5. Attack Label Taxonomy

### Complete label inventory (22 distinct labels, from parsed label files)

| Phase | Labels | Count | Primary Log Source |
|-------|--------|------:|-------------------|
| **Initial Access** | `attacker_vpn`, `foothold` | 28 + 8,724 | vpn/openvpn.log |
| **Reconnaissance** | `traceroute`, `dns_scan`, `network_scan`, `service_scan` | 4 + 414 + 92 + 455 | inet-firewall/dnsmasq.log, intranet_server/apache |
| **Web Enumeration** | `dirb`, `wpscan`, `attacker_http` | 4,493 + 3,207 + 7,723 | intranet_server/apache2 |
| **Exploitation** | `webshell_upload`, `webshell_cmd` | 3 + 44 | intranet_server/apache2 |
| **Password Cracking** | `crack_passwords` | 49 | monitoring/cpu.log |
| **Privilege Escalation** | `escalate`, `attacker_change_user`, `escalated_command`, `escalated_sudo_command`, `escalated_sudo_session` | 82 + 8 + 10 + 10 + 3 | intranet_server/auth.log, audit.log |
| **Exfiltration** | `dnsteal`, `dnsteal-received`, `dnsteal-dropped`, `exfiltration-service`, `attacker` | 53,056 + 53,006 + 48 + 2 + 53,056 | inet-firewall/dnsmasq.log, internal_share/audit.log |

Note: `attacker` is a generic co-label that appears on all 53,056 dnsteal entries. Labels by attack phase: exfiltration (53,056 lines), foothold (8,789 lines), escalate (17 lines).

### Top 5 Label Co-Occurrence Patterns

| Label Combination | Count |
|-------------------|------:|
| attacker + dnsteal + dnsteal-received | 53,006 |
| attacker_http + dirb + foothold | 4,485 |
| attacker_http + foothold + wpscan | 3,199 |
| foothold + service_scan | 447 |
| dns_scan + foothold | 414 |

### Attack Chain (from attacks.log, 55 steps)

```
VPN connect → traceroute → DNS brute force → host discovery (DMZ + local)
→ service scan → dirb scan → WPScan → webshell upload → reverse shell
→ password cracking (~37 min) → privilege escalation (su to jhall) → sudo commands
→ data exfiltration via DNSteal (runs independently until 13:50)
```

---

## 6. Selected Data Scope for Project

### Why These Files

We select all 8 labeled log files from the full "scenario" configuration. Rationale:

1. **They have ground truth** — we can validate queries against known attack events
2. **They cover the full attack chain** — from initial VPN access through exfiltration
3. **They span 5 hosts** — enables cross-host correlation (a key analytical capability)
4. **They total 295,243 rows** — nearly 6x the minimum requirement
5. **They require normalization** — 7 different log formats (text, syslog, key-value, JSON)
6. **They are the richest scenario** — using a reduced scenario would lose labeled data

### Log-by-Log Selection Rationale

| # | Log | Rows | Why Selected |
|---|-----|-----:|-------------|
| 1 | **DNS (dnsmasq.log)** — inet-firewall | 275,900 | Largest log. Contains DNS exfiltration (DNSteal encodes stolen data in subdomain queries to `*.email-19.kennedy-mendoza.info`). 53,054 exfiltration rows identifiable by domain pattern. Normal DNS traffic provides baseline for anomaly detection. |
| 2 | **Apache access.log** — intranet_server | 8,530 | 90% labeled. Shows full web attack: service scan → dirb (4,485 lines) → WPScan (3,199 lines) → webshell upload → command execution. Rich HTTP fields (method, path, status, user-agent). |
| 3 | **Apache error.log** — intranet_server | 36 | 100% labeled. Short but every line is attack-related (directory traversal attempts, script probing). Correlates with access log. |
| 4 | **VPN (openvpn.log)** — vpn | 5,537 | Entry point of attack. Shows attacker VPN connection (32 events from IP 192.168.230.122) among legitimate remote employees (jhall: 2,202 events, twhite: 1,172, ahayes: 909). |
| 5 | **Audit log** — intranet_server | 2,316 | Kernel-level syscall data. 11 event types. Captures privilege escalation (su to user, credential access). |
| 6 | **Auth log** — intranet_server | 272 | Authentication events. 132 session opens, 131 session closes, 5 for user jhall (attacker's target), 1 su success. |
| 7 | **CPU monitoring** — monitoring | 1,920 | Time-series metric (45-sec intervals). CPU spikes to 100% during password cracking window (03:59–04:36). Normal baseline ~6.4%. |
| 8 | **Audit log** — internal_share | 732 | Captures exfiltration service start on file server. Only 2 labeled lines but establishes lateral movement. 106 cron sessions provide normal baseline. |

### Cross-Log Correlation (Validated)

The attacker is traceable across multiple log sources:

| Log Source | Attacker Events | Attacker IP Used | Time Range |
|-----------|----------------:|-----------------|------------|
| DNS (dnsmasq) | 53,054+ | 172.19.131.174 (VPN-internal) | Jan 21–24 (exfiltration runs over days) |
| Apache Access | web attack events | 172.19.131.174 (VPN-internal) | Jan 24 03:57–04:37 |
| Apache Error | 36 | 172.19.131.174 (VPN-internal) | Jan 24 03:57 |
| OpenVPN | 32 | 192.168.230.122 (external) | Jan 24 03:01–04:42 |

5 IPs are shared between DNS and Apache logs, enabling join-based correlation.

### What We're NOT Including (and why)

| Excluded | Reason |
|----------|--------|
| Suricata eve.json (inet-firewall, vpn, intranet_server) | ~600K lines total but NO ground truth labels. Useful for research scope later, not class project. |
| DNS (inet-dns/dnsmasq.log) | 264,982 lines but NO labels. Mirror of inet-firewall DNS. |
| Employee state machine logs (sm.log, 10 users, ~68K events) | No labels. Captures normal user behavior (email, file browsing). Useful as behavioral baseline but not needed for project scope. |
| Mail server logs | No labels. Not part of attack chain. |
| Cloud share logs | No labels. Not part of attack chain. |

---

## 7. Planned Schema (PostgreSQL)

### Tables (8 tables, exceeds minimum of 4)

```
hosts                   ← Dimension: 22 hosts with metadata (from servers.yaml)
log_events              ← Fact: all 295,243 parsed log events
attack_labels           ← 61,862 label records linked to log_events
http_events             ← Parsed fields from Apache access/error logs (8,566 rows)
dns_events              ← Parsed fields from dnsmasq logs (275,900 rows)
auth_events             ← Parsed fields from auth.log + audit.log (3,320 rows)
vpn_events              ← Parsed fields from openvpn.log (5,537 rows)
system_metrics          ← Parsed fields from cpu.log (1,920 rows)
```

### Relationships

```
hosts (1) ──────< (N) log_events
log_events (1) ──< (N) attack_labels
log_events (1) ──── (1) http_events      (for log_type = 'http')
log_events (1) ──── (1) dns_events       (for log_type = 'dns')
log_events (1) ──── (1) auth_events      (for log_type = 'auth')
log_events (1) ──── (1) vpn_events       (for log_type = 'vpn')
log_events (1) ──── (1) system_metrics   (for log_type = 'metrics')
```

### Why This Design Requires Normalization

Raw state: each log type is a flat text file with completely different schemas. Loading them as-is would be one giant denormalized table with NULLs everywhere (a DNS event has no HTTP status code; an HTTP event has no DNS query domain).

Normalization steps:
- **1NF**: Parse multi-value fields (labels array → separate rows in attack_labels)
- **2NF**: Separate host metadata from event data (host info repeated on every log line)
- **3NF**: Remove transitive dependencies (log_type-specific fields into sub-tables; attack_phase derived from label_name into a lookup)

### Attribute Inventory (46 unique across tables)

| Table | Key Attributes |
|-------|----------------|
| hosts | host_id, hostname, ip_address, host_type, network_zone, os_distribution |
| log_events | event_id, host_id (FK), timestamp, log_type, source_file, line_number |
| attack_labels | label_id, event_id (FK), label_name, attack_phase, rule_name |
| http_events | event_id (FK), client_ip, method, path, query_string, protocol, status_code, bytes_sent, user_agent, referer |
| dns_events | event_id (FK), action, query_type, domain, client_ip, extra_ip, is_exfiltration |
| auth_events | event_id (FK), event_type, username, service, pid, uid, acct, exe, op, result, terminal |
| vpn_events | event_id (FK), user, client_ip, client_port, message, event_type |
| system_metrics | event_id (FK), hostname, cpu_total_pct, cpu_user_pct, cpu_system_pct, cpu_idle_pct, cpu_iowait_pct, cores |

### SQL Type Mapping (from parsed data)

| Column | SQL Type | Source |
|--------|----------|--------|
| timestamp | TIMESTAMP WITH TIME ZONE | All logs |
| client_ip, extra_ip | VARCHAR(45) | DNS, Apache, VPN |
| domain | VARCHAR(309) | DNS (long due to exfil-encoded subdomains) |
| status_code | INTEGER | Apache access |
| bytes_sent | INTEGER | Apache access |
| path, user_agent, message | TEXT | Apache, VPN, Auth |
| method, protocol, action, event_type | VARCHAR(20) | Various |
| cpu_*_pct | DECIMAL(10,4) | CPU monitoring |
| is_exfiltration | BOOLEAN | DNS |

---

## 8. Analytical Questions (for SQL queries)

These tie directly to the project problem ("Can we detect and reconstruct a cyber attack from distributed logs?"):

**Basic:**
1. How many events per host per day? (GROUP BY, COUNT)
2. What HTTP status codes appear during the attack window vs. normal? (WHERE, GROUP BY)
3. Which users authenticated during the attack? (JOIN hosts + auth_events)
4. Top 10 most queried DNS domains? (ORDER BY, LIMIT)
5. Event count by log type? (GROUP BY)
6. VPN connections by hour of day? (date functions, GROUP BY)
7. HTTP requests by method and path prefix? (GROUP BY, LIKE)
8. Attack labels by phase? (GROUP BY on attack_labels)

**Advanced:**
1. Reconstruct attack timeline ordered by timestamp across all 8 log types (CTE + UNION + ORDER BY)
2. Running count of events per host leading up to the attack (window function: COUNT OVER)
3. DNS query frequency per minute — detect the exfiltration spike on 275,900 DNS rows (window function + time bucketing)
4. Simulate scenario1 vs scenario2 — filter labels by rule source to show what would be visible under each scenario (subquery + conditional aggregation)
5. CPU anomaly detection — find the password-cracking window where cpu_total_pct jumps from ~6.4% baseline to 100% (self-join or LAG window function on 1,920 metric samples)
6. Cross-host attacker trace — join log_events + hosts on IP to track attacker across 4 log sources (multi-table JOIN with IP matching across both attacker IPs)
7. Create a view for the attack dashboard (CREATE VIEW combining log_events + attack_labels + hosts)
8. Index performance comparison on timestamp queries (CREATE INDEX + EXPLAIN ANALYZE)

---

## 9. Parsing Considerations

Each log type needs a separate parser. All parsers have been implemented and validated (see `analysis/parsers/`):

| Log Type | Format | Parsing Approach | Fields Extracted | Parse Errors |
|----------|--------|-----------------|-----------------|-------------|
| Apache access | Combined Log Format | Regex: client, timestamp, request, status, bytes, referer, user-agent | 12 fields | 0 |
| Apache error | `[timestamp] [module:level] [pid] [client] message` | Regex with bracketed groups | 7 fields | 0 |
| dnsmasq | Syslog: `Mon DD HH:MM:SS dnsmasq[pid]: action domain from/to/is IP` | Regex + action classification | 8 fields + is_exfiltration flag | 0 |
| auth.log | Syslog: `Mon DD HH:MM:SS hostname process[pid]: message` | Syslog regex + sub-regex for user/action extraction | 8 fields | 0 |
| audit.log | `type=X msg=audit(epoch:serial): key=value pairs` | Regex for header + key-value parser (handles nested `msg='...'` blocks) | 16 fields | 0 |
| openvpn.log | `YYYY-MM-DD HH:MM:SS user/ip:port message` | Regex + event type classification (9 categories) | 7 fields | 0 |
| cpu.log | JSON (Logstash/Metricbeat output) | `json.loads()` + nested field extraction | 10 fields | 0 |
| Labels (JSONL) | `{"line": N, "labels": [...], "rules": {...}}` | `json.loads()` per line, phase derivation from label-to-phase mapping | 7 fields | 0 |

---

## 10. Key Risks and Mitigations

| Risk | Impact | Mitigation |
|------|--------|------------|
| DNS log is 93.4% of total rows — skews analysis | Unbalanced tables | Analyze DNS separately; use sampling for dashboards; partition by is_exfiltration flag |
| Some tables are small (auth: 272, error: 36) | May look like toy data | Emphasize that these are real logs from a simulation; small count = real enterprise auth volume over 4 days |
| Timestamp formats differ across 7 log types | Parsing complexity | All parsers normalize to UTC datetime; map to TIMESTAMP WITH TIME ZONE during ingestion |
| Labels reference line numbers, not timestamps | Must preserve line numbers during parsing | Add `line_number` column to every parsed table; join labels on (source_file, line_number) |
| Audit log has nested key-value pairs | Complex parsing | Recursive key-value parser handles nested `msg='op=... acct="..." ...'` blocks; extract only fields needed for schema |
| Attacker uses 2 different IPs | Cross-log correlation requires matching both IPs | Both IPs (192.168.230.122 external, 172.19.131.174 VPN-internal) loaded from `attacker.yaml` config; JOIN conditions must match on either |
| Scenario confusion | Team might not understand that labels are scenario-dependent | Document that `labels/` corresponds to full scenario; note which rules are active per scenario in schema documentation |
